        /*이 부분 추가했습니다*/
        #login{
            font-family:"Hahmlet", serif; /*이거 글씨체 안 바낀 곳에 추가로 넣어주세요*/
            margin-top:100px;
            margin-bottom:100px;
        }
       body{
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
            margin: 0;
            padding: 0;
            text-align: center;    
        }
      
        h2{
            font-size: 40px;/*이 부분 추가했습니다*/
            color:rgb(60, 60, 95);/*이 부분 추가했습니다*/
            margin-bottom:80px;/*이 부분 추가했습니다*/
        }
     
        .signup-container{
            font-size:18px;/*이 부분 추가했습니다*/
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 800px;
            height: auto;
            background-color: rgba(177, 176, 176, 0.205);/*이 부분 추가했습니다*/
           
        }
        
     
        .form-tabs {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid #ddd;
            padding-bottom: 10px;
             margin: 0 auto;
            margin-top: 30px;
            margin-bottom: 30px;
            width: 80%;
           
        }
 

        .form-tabs div:last-child:before {
           content: '';
           display: inline-block;
           width: 8px;
           height: 8px;
           background-color: red;
           border-radius: 50%;
           margin-right: 15px;
           margin-bottom: 3.5px;
        }
.form-group .zipcode-container {
    display: flex;
    justify-content: flex-start; 
    align-items: center; 
    width: 66%; 
    gap: 5px; 
}
.form-group label {
    font-weight: bold; 
    margin-top: 8px;
}
.form-group .zipcode-container input {
    padding-right: 10px;
    width: 30%; 
}

.form-group .zipcode-container button {
    padding: 5px 10px;
    font-size: 14px;
    border: 1px solid #ccc;
    border-radius: 4px;
    cursor: pointer;
    background-color:  #ccc; ; 
    transition: background-color 0.3s; 
}

.form-group .zipcode-container button:hover {
    background-color: #999; 
}




.form-group {
    
    display: flex;
    align-items: flex-start; /* 라벨과 입력 필드 정렬 유지 */
    gap: 10px; /* 입력 필드와 라벨 간 간격 */
    margin-bottom: 45px; /* 그룹 간 간격 */
    position: relative; /* 경고 메시지 위치 설정 기준 */
    margin-left: 50px;
    
}

.error-message {
    color: red;
    font-size: 12px; /* 작은 글씨 크기 */
    position: absolute; /* 위치를 절대값으로 설정 */
    top: 100%; /* 입력 필드 바로 아래에 배치 */
    left: 0; /* 입력 필드와 같은 시작 위치 */
    display: none; /* 기본적으로 숨김 */
    white-space: nowrap; /* 메시지를 한 줄로 표시 */
    pointer-events: none; /* 클릭 시 영향을 주지 않음 */
    text-align: center;
    margin-left: 180px;
}




.form-group label {
    width: 30%; 
    text-align: left;
    font-size: 14px;
    line-height: 1.5;
}

.form-group input,
.form-group select {
    width: 50%;
    border: none;
    border-bottom: 2px solid #ccc; 
    background-color: transparent;
    outline: none;
    padding: 5px 0;
    font-size: 14px;
    text-align: left; 
}
.phone-prefix {
    width: 50px !important;
    font-weight: bold;
    border: none;
    border-bottom: 2px solid #ccc;
    background-color: transparent;
    text-align: center; 
    cursor: not-allowed;
}

.form-group input:focus {
    border-bottom: 2px solid #007BFF; 
}

.input-container {
    display: flex;
    gap: 10px; 
    width: 40%;
}

.input-container input[type="number"] {
    text-align: center; 
    border: none;
    border-bottom: 2px solid #ccc; 
    background-color: transparent;
    width: 80px; 
}
.terms-container{
            border-bottom: 2px solid gray;
            display: flex;
            justify-content: center;
            align-items: center;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

 .button-group {
    display: flex;
    justify-content: center; 
    gap: 10px; 
    margin-top: 40px; 
    padding: 20px 20px;
    border-radius: 50px;
}

.button-group button {
    padding: 10px 20px; /* 버튼 내부 여백 */
    border: 2px solid #ccc; /* 테두리 색상 */
    border-radius: 50px; /* 버튼 모서리를 둥글게 */
    font-size: 16px; /* 글자 크기 */
    font-weight: bold; /* 글자 굵기 */
    cursor: pointer;
    transition: all 0.3s; /* hover 효과를 부드럽게 */
}
.cancel-btn {
    background-color: transparent; /* 투명 배경 */
    color: #333; /* 글자 색상 */
}


button {
    padding: 10px 10px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 14px;
}
button.check-btn {
    height: 38px; /* 입력 필드와 동일한 높이 */
    padding: 0 15px; /* 버튼 내부 여백 */
    font-size: 14px; /* 글씨 크기 */
    border: 1px solid #ccc;
    border-radius: 5px;
    background-color: #ccc;
    color: #fff;
    cursor: pointer;
    transition: background-color 0.3s ease; /* hover 효과 */
}
input#email {
    width: 30%; /* 다른 필드와 동일한 폭으로 설정 */
    padding: 5px 10px;
    border: none;
    border-bottom: 2px solid #ccc;
    background-color: transparent;
    outline: none;
    font-size: 14px;
    margin-top: 5px; /* 불필요한 여백 제거 */
    text-align: left;
    height: 15px; /* 다른 입력 필드와 동일한 높이 */
}

button.check-btn:hover {
    background-color: #999; 
}
input#emailNum{
    width: 80px; 
}
input#address {
    width: 50%; /* 다른 필드와 동일한 폭 설정 */
    padding: 5px 10px;
    margin-right: 40px;
    border: none;
    border-bottom: 2px solid #ccc;
    background-color: transparent;
    outline: none;
    font-size: 14px;
    text-align: left;
    margin-top: 5px;
}
.cancel-btn {
    background-color: #ccc;
    color: #fff;
    transition: background-color 0.3s;
    width: 50%;
}

.cancel-btn:hover {
    background-color: #999;
}

.join-btn {
    background-color: #007BFF;
    color: #fff;
    width: 50%;
    transition: background-color 0.3s;
}
span#required{
    color: red;
}
.join-btn:hover {
    background-color: #0056b3;
}
textarea{
    width: 400px;
    border: 1px solid rgba(0, 0, 0, 0.2);
    border-radius: 15px;
    height: 300px;
}
textarea#agreeBox{
    border-radius: 15px;
    width: 400px;
    height: 30px;
    border: 1px solid rgba(0, 0, 0, 0.2);
}
label.required::before {
    content: '';
    display: inline-block;
    width: 8px;
    height: 8px;
    background-color: red; 
    border-radius: 50%;
    margin-right: 5px; 
    vertical-align: middle;
    color: initial;
    background-clip: content-box; 
}
